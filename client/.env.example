#For Local Dev. The code will append /api/plants, /api/species, etc. to this URL.
API_URL=http://localhost:3003

#Production will use the .env.production file automatically when we call "npm run build".